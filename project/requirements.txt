torch
torchvision
numpy
opencv-python

# Optional, only needed for specific paths (see README "Setup"):
# pydicom     - only if your raw images are .dcm
# optuna      - only for --tuning-method bayesian (default is grid)
# matplotlib  - only for models/diagram.py's architecture diagram
